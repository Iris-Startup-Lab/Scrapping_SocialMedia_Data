# =============================================================================
# ChismesitoGPT v2 — requirements.txt
# Instalar: pip install -r requirements.txt
# =============================================================================

# ─── UI Framework ──────────────────────────────────────────────────────────
# Fijado para reproducibilidad en HuggingFace Spaces (debe coincidir con sdk_version del README).
gradio==6.19.0

# ─── HuggingFace Spaces — ZeroGPU ──────────────────────────────────────────
# Habilita el decorador @spaces.GPU (clasificación zero-shot acelerada en A100).
# Inofensivo en local; en HF ZeroGPU es obligatorio.
spaces

# ─── Agent Orchestration ──────────────────────────────────────────────────
langchain>=0.2.0

# ─── LLMs — APIs oficiales ─────────────────────────────────────────────────
google-genai>=1.0                           # Gemini (https://ai.google.dev)
openai>=1.0                                 # DeepSeek (OpenAI-compatible)
requests>=2.31                              # Claude (Anthropic Messages API)

# ─── Base de Datos ─────────────────────────────────────────────────────────
supabase>=2.0
pgvector
psycopg2-binary
sqlalchemy

# ─── Data Processing ───────────────────────────────────────────────────────
pandas>=2.0
numpy>=1.24

# ─── Visualización ─────────────────────────────────────────────────────────
plotly>=5.0
matplotlib>=3.7
seaborn>=0.12

# ─── Exportación ───────────────────────────────────────────────────────────
openpyxl>=3.1
xlsxwriter>=3.1

# ─── NLP / Análisis de Texto ───────────────────────────────────────────   ────
# Emoción (y sentimiento derivado de ella) — un solo modelo RoBERTuito:
#   - En local/CPU:  librería pysentimiento (mejor preprocesado nativo).
#   - En HF Spaces:  el MISMO modelo cargado vía transformers (device=-1, ZeroGPU-safe).
# Zero-shot de categorías: MoritzLaurer/mDeBERTa-v3-base-mnli-xnli (acelerado con @spaces.GPU en HF).
pysentimiento>=0.7                           # ruta local (sentimiento/emoción español)
transformers>=4.35                           # pipelines: emotion (HF) + zero-shot classifier
torch>=2.0                                   # backend de transformers

# ─── Social Media APIs ─────────────────────────────────────────────────────
praw>=7.0                                    # Reddit
google-api-python-client>=2.0                # YouTube Data API v3
googlemaps>=4.10                             # Google Maps Places API
google-play-scraper>=1.2                     # Play Store reviews
tweepy>=4.14                                 # Twitter / X

# ─── Web Scraping & Search ─────────────────────────────────────────────────
beautifulsoup4>=4.12
google-search-results>=2.4                   # SerpAPI
ddgs>=6.0                                    # DuckDuckGo (búsqueda gratis, último recurso)
apify-client>=1.0.0
aiohttp>=3.9                                 # Async HTTP
httpx>=0.25

# ─── Utilidades ────────────────────────────────────────────────────────────
python-dotenv>=1.0                           # Carga de .env
deep-translator>=1.11                        # Traducción / detección idioma

# ─── spaCy (detección de entidades: sustantivos, verbos, etc. — uso futuro) ──
# Antes venía como dependencia de pysentimiento; ahora se declara explícito.
spacy>=3.8,<3.9                              # debe ser 3.8.x para casar con el modelo de abajo
https://github.com/explosion/spacy-models/releases/download/es_core_news_md-3.8.0/es_core_news_md-3.8.0-py3-none-any.whl